<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.czkuo.rdf88701.infra.mapper.GripperTaskHistoryMapper">
    <!-- 2026-06-24 ver XML 狀態：已檢查，XML 註解與現有 SQL 結構相符。 -->

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.czkuo.rdf88701.infra.entity.GripperTaskHistory">
        <id column="id" property="id" />
        <result column="origin_id" property="originId" />
        <result column="gripper_id" property="gripperId" />
        <result column="task_type" property="taskType" />
        <result column="task_status" property="taskStatus" />
        <result column="container_main_id" property="containerMainId" />
        <result column="target_height_mm" property="targetHeightMm" />
        <result column="layer_count" property="layerCount" />
        <result column="dispatched_time" property="dispatchedTime" />
        <result column="completed_time" property="completedTime" />
        <result column="cancelled_time" property="cancelledTime" />
        <result column="cancelled_reason" property="cancelledReason" />
        <result column="operator" property="operator" />
        <result column="remark" property="remark" />
        <result column="change_type" property="changeType" />
        <result column="archived_time" property="archivedTime" />
        <result column="archived_by" property="archivedBy" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, origin_id, gripper_id, task_type, task_status, container_main_id, source_location_id, target_location_id, target_height_mm, layer_count, dispatched_time, completed_time, cancelled_time, cancelled_reason, operator, remark, change_type, archived_time, archived_by
    </sql>

    <!-- 新增資料：b at ch In se rt。 -->
    <insert id="batchInsert">
        INSERT INTO gripper_task_history (
            origin_id, gripper_id, task_type, task_status, priority_level,
            container_main_id, from_location_id, to_location_id,
            target_height_mm, layer_count, dispatched_time, completed_time,
            done_time, cancelled_time, cancelled_reason, operator, remark,
            change_type, archived_time, archived_by
        )
        VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.originId}, #{item.gripperId}, #{item.taskType}, #{item.taskStatus}, #{item.priorityLevel},
                #{item.containerMainId}, #{item.fromLocationId}, #{item.toLocationId},
                #{item.targetHeightMm}, #{item.layerCount}, #{item.dispatchedTime}, #{item.completedTime},
                #{item.doneTime}, #{item.cancelledTime}, #{item.cancelledReason}, #{item.operator}, #{item.remark},
                #{item.changeType}, #{item.archivedTime}, #{item.archivedBy}
            )
        </foreach>
    </insert>

</mapper>
