<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.czkuo.rdf88701.infra.mapper.HmiDisplayTaskMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.czkuo.rdf88701.infra.entity.HmiDisplayTask">
        <id column="id" property="id" />
        <result column="tid" property="tid" />
        <result column="msg_ch" property="msgCh" />
        <result column="msg_en" property="msgEn" />
        <result column="status" property="status" />
        <result column="attempts" property="attempts" />
        <result column="last_error" property="lastError" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
        <result column="sent_at" property="sentAt" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, tid, msg_ch, msg_en, status, attempts, last_error, created_at, updated_at, sent_at
    </sql>

</mapper>
