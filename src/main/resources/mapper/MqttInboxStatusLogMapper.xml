<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.czkuo.rdf88701.infra.mapper.MqttInboxStatusLogMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.czkuo.rdf88701.infra.entity.MqttInboxStatusLog">
        <id column="id" property="id" />
        <result column="inbox_id" property="inboxId" />
        <result column="from_state" property="fromState" />
        <result column="to_state" property="toState" />
        <result column="changed_by" property="changedBy" />
        <result column="change_reason" property="changeReason" />
        <result column="change_time" property="changeTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, inbox_id, from_state, to_state, changed_by, change_reason, change_time
    </sql>

</mapper>
